const name = "Guido";
const surname = "Rimati";
const age = 37;

const person = {
    name,
    surname,
    age
};

console.log(`Mi nombre es ${person.name} ${person.surname} y mi edad es ${person.age}`);